// filter
// This method creates a new array with only elements that passes the condition inside the provided function.

const arr = [1, 2, 3, 4, 5];
const mapped = arr.filter(e  => e === 2 || e === 5);
console.log(mapped)
